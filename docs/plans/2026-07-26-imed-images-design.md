# IMED Images Design

Add a dedicated `## IMED` section to `list.md`, immediately after the existing IMED project link. Keep the presentation in standard Markdown: a short introduction followed by the issue tree and hypothesis tree at their natural page width.

Show the issue tree first because it frames the questions, followed by the hypothesis tree containing the proposed answers. Give each image a concise descriptive heading and make each displayed image link to its full-resolution PNG in `assets/`.

No custom HTML, styling, or additional site changes are needed.
